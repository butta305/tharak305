    
    // with argument no return
    
    function argname(name)      // argument
    {
        console.log("Hi " + name);
    }

    argname("keshu");

    let k = argname("kusuma");
    console.log(k);